@model IEnumerable<Invoices.DAL.Models.Product>
@{
    ViewData["Title"] = "Product list";
}
<div class="container p-3">  
    <div class="row pt-4">   
        <div class="col-6">
            <h2 class="text-primary">Product List</h2>
        </div>
    </div>
</div>
<br/><br/>

<table class="table table-bordered table-striped" style="width:100%">
    <thead>
        <tr>
            <th>
                Product name
            </th>
            <th>
                Price
            </th>
        </tr>
    </thead>
    <tbody>



        @foreach (var obj in Model)
        {
            <tr>
                <th>
                    <h1>Store items</h1>
                </th>
            </tr>
        }

      @*  @foreach (var obj in Model.Products)
        {
                <tr>
                    <td width="50%">
                    @obj
                    </td>
                    <td width="30%">
                    @obj
                    </td>
                    <td>
                        <div class = "w-75 btn-group" role="group"></div>
                        <a asp-controller="Category" asp-action="Edit" asp-route-id="@obj"
                        class="btn btn-primary mx-2"> <i class="bi bi-pencil-square"></i>Edit
                        </a>

                         <a asp-controller="Category" asp-action="Delete" asp-route-id="@obj"
                        class="btn btn-danger mx-2"><i class="bi bi-trash3-fill"></i>Delete
                        </a>
                    </td>
                </tr>
        }*@
    </tbody>
</table>
